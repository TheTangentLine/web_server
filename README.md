# Web Server
Build a Web server using C++ from scratch.

# Socket

A Socket is a mechanism that OS provide so as to give programs access to network. It allows messages to be sent and received between applications on different networked machines.